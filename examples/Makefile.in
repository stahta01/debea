# =========================================================================
#     This makefile was generated by
#     Bakefile 0.2.8 (http://www.bakefile.org)
#     Do not modify, all changes will be overwritten!
# =========================================================================


@MAKE_SET@

prefix = @prefix@
exec_prefix = @exec_prefix@
datarootdir = @datarootdir@
INSTALL = @INSTALL@

### Variables: ###

DESTDIR = 

### Conditionally set variables: ###


### Targets: ###

all: bind bindstb csv_manual csv_auto filter idlock inheritance quickstart sublists transaction xmlsimple queries

install: 

uninstall: 

install-strip: install

clean: 
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	-(cd bind && $(MAKE) clean)
	-(cd bindstb && $(MAKE) clean)
	-(cd csv_manual && $(MAKE) clean)
	-(cd csv_auto && $(MAKE) clean)
	-(cd filter && $(MAKE) clean)
	-(cd idlock && $(MAKE) clean)
	-(cd inheritance && $(MAKE) clean)
	-(cd quickstart && $(MAKE) clean)
	-(cd sublists && $(MAKE) clean)
	-(cd transaction && $(MAKE) clean)
	-(cd xmlsimple && $(MAKE) clean)
	-(cd queries && $(MAKE) clean)

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile
	-(cd bind && $(MAKE) distclean)
	-(cd bindstb && $(MAKE) distclean)
	-(cd csv_manual && $(MAKE) distclean)
	-(cd csv_auto && $(MAKE) distclean)
	-(cd filter && $(MAKE) distclean)
	-(cd idlock && $(MAKE) distclean)
	-(cd inheritance && $(MAKE) distclean)
	-(cd quickstart && $(MAKE) distclean)
	-(cd sublists && $(MAKE) distclean)
	-(cd transaction && $(MAKE) distclean)
	-(cd xmlsimple && $(MAKE) distclean)
	-(cd queries && $(MAKE) distclean)

bind: 
	(cd bind && $(MAKE) all)

bindstb: 
	(cd bindstb && $(MAKE) all)

csv_manual: 
	(cd csv_manual && $(MAKE) all)

csv_auto: 
	(cd csv_auto && $(MAKE) all)

filter: 
	(cd filter && $(MAKE) all)

idlock: 
	(cd idlock && $(MAKE) all)

inheritance: 
	(cd inheritance && $(MAKE) all)

quickstart: 
	(cd quickstart && $(MAKE) all)

sublists: 
	(cd sublists && $(MAKE) all)

transaction: 
	(cd transaction && $(MAKE) all)

xmlsimple: 
	(cd xmlsimple && $(MAKE) all)

queries: 
	(cd queries && $(MAKE) all)


# Include dependency info, if present:
@IF_GNU_MAKE@-include ./.deps/*.d

.PHONY: all install uninstall clean distclean bind bindstb csv_manual csv_auto \
	filter idlock inheritance quickstart sublists transaction xmlsimple queries
